steps:
  - name: gcr.io/cloud-builders/docker
    args:
      [
        "build",
        "-t",
        "$_IMAGE",
        "-f",
        "docker/Dockerfile.prod.api",
        "."
      ]
images:
  - "$_IMAGE"

